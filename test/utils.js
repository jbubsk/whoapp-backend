var assert = require('assert'),
    chai = require('chai'),
    expect = chai.expect,
    should = chai.should();

describe('utils test', function () {
    var utils = require('../src/utils'),
        moment = require('moment'),
        result;

    //result = utils.getFormattedDate(new Date());
    //
    //expect(result).be.equal('2015-06-15');
});